{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PreBuild",
            "command": "dotnet build",
            "type": "shell",
            "group": "none",
            "options": {
                "cwd": "${workspaceFolder}/PoshSSH"
            },
            "presentation": {
                "reveal": "always",
                "clear": true
            },
            "problemMatcher": {
                "base": "$msCompile",
                "fileLocation": [
                    "relative",
                    "${workspaceFolder}/PoshSSH"
                ]
            }
        },
        {
            "label": "PostBuild",
            "command": "$watch = '${workspaceFolder}/Posh_SSH.dll'; While ( & {try { [IO.File]::OpenWrite($watch).close(); $false } catch {'Lock exists for {0}' -f $watch | Write-Host; $true}} ) { Start-Sleep -Seconds 1 }; Copy-Item -Path '${workspaceFolder}/PoshSSH/bin/Debug/netstandard2.0/Posh_SSH.dll' -Destination ${workspaceFolder} -Force",
            "type": "shell",
            "group": "build",
            "dependsOn": "PreBuild",
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": [
                "$pester"
            ]
        }
    ]
}